spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost/pcsin
    username: postgres
    password: "#koldin13579"
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        default_batch_fetch_size: 100
      # show_sql: true
      format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080

jwt:
  accessSecret: 'asoiwaojiw'
  refreshSecret: 'aosijdwaoij'